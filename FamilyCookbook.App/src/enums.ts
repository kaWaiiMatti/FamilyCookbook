export enum MealFreshness
{
  FreshMade = 20,
  FridgeStored = 40,
  FreezerStored = 60
}

export enum MealSource {
  SelfMade = 20,
  MadeByOthers = 40,
  ReadyMade = 60,
  RestaurantPickUp = 80,
  RestaurantDineIn = 100,
}

export enum MealStatus
{
  Planned = 20,
  Eaten = 40,
}

export enum MealType
{
  Breakfast = 20,
  Lunch = 40,
  Dinner = 60,
}
